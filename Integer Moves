#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
  int t;
  cin >> t;
  while (t--) {
    int x, y;
    cin >> x >> y;
    int d = x * x + y * y;
    if(x==0 && y==0)
    cout<<0<<endl;
    else if(sqrt(d) != ceil(sqrt(d)))
    cout<<2<<endl;
    else
    cout<<1<<endl;
  }
}
