#include <bits/stdc++.h>

using namespace std;

int main() {
  int t;
  cin >> t;
  int ans=0;
  while (t--) {
      string s="";
      cin>>s;
      if((s[0]=='X' && s[1]=='+') || s[0]=='+')
      ans++;
      else if((s[0]=='X' && s[1]=='-') || s[0]=='-')
      ans--;
  }
  cout<<ans;
}
